import { ThemeSpaces } from '../../theme.types';

const space = 2;
export const defaultThemeSpaces: ThemeSpaces = {
  $space: {
    xs: space,
    sm: space * 2,
    md: space * 4,
    lg: space * 8,
    xl: space * 16,
  }
};
